{% extends 'admin/base_admin.html.twig' %}

{% block title %}Création administrateur{% endblock %}

{% block body %}
    <div class="card shadow-lg rounded-4 p-5 w-100" style="max-width: 400px;">
        <div class="text-center mb-4">
            <img src="{{ asset('pictures/section/logo_admin.png') }}" alt="Logo" width="80">
            <h2 class="mt-3 fw-bold">Création d'un administrateur</h2>
            <p class="text-muted">Veuillez remplir ces champs</p>
        </div>

        {{ form_start(registrationForm) }}
        {{ form_errors(registrationForm) }}

        <div class="form-floating mb-3">
            {{ form_widget(registrationForm.name) }}
            {{ form_label(registrationForm.name) }}
        </div>

        <div class="form-floating mb-3">
            {{ form_widget(registrationForm.first_name) }}
            {{ form_label(registrationForm.first_name) }}
        </div>

        <div class="form-floating mb-3">
            {{ form_widget(registrationForm.email) }}
            {{ form_label(registrationForm.email) }}
        </div>

        <div class="form-floating mb-3">
            {{ form_widget(registrationForm.plainPassword) }}
            {{ form_label(registrationForm.plainPassword) }}
        </div>

        <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

        <div class="d-grid">
            <button type="submit" class="btn btn-primary btn-lg">
                🔒 Inscription
            </button>
        </div>
        {{ form_end(registrationForm) }}
    </div>
{% endblock %}
