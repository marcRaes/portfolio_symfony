{% extends 'admin/base_admin.html.twig' %}

{% block title %}Réinitialisez votre mot de passe{% endblock %}

{% block body %}
    <div class="card shadow-lg rounded-4 p-5 w-100" style="max-width: 400px;">
        {% for flash_error in app.flashes('reset_password_error') %}
            <div class="alert alert-danger" role="alert">{{ flash_error }}</div>
        {% endfor %}
        <div class="text-center mb-4">
            <img src="{{ asset('pictures/section/logo_admin.png') }}" alt="Logo" width="80">
            <h2 class="mt-3 fw-bold">Réinitialisez votre mot de passe</h2>
            <p class="text-muted">Entrez votre adresse e-mail afin de recevoir le lien de reinitialisation de votre mot de passe.</p>
        </div>

        {{ form_start(requestForm) }}
            <div class="form-floating mb-3">
                {{ form_widget(requestForm.email) }}
                {{ form_label(requestForm.email) }}
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-primary">
                    🔒 Réinitialiser le mot de passe
                </button>
            </div>

        {{ form_end(requestForm) }}
    </div>
{% endblock %}
